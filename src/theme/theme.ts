import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#f0f8ff" },
    secondary: {
      main: "#98c9f4",
    },
  },
  typography: { 
    fontFamily: "Monserrat"
  },
});
